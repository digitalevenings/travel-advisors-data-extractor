# =====================================
# 🔧 Webshare Proxy Configuration
# =====================================

# Your Webshare.io API key for proxy rotation.
# Get a FREE account (referral link) → https://www.webshare.io/?referral_code=f2etuk0lcc0l
# Free tier includes 10 proxies - no credit card required!
WEBSHARE_PROXY_API_KEY = your-webshare-api-key

# =====================================
# ⚙️ Scraper Configuration
# =====================================
CACHE_TTL = 604800              # Cache TTL in seconds (default: 7 days)
PAGE_SIZE = 500                 # Number of items per page
MAX_RETRIES = 3                 # Max retry attempts for failed requests
BATCH_SIZE = 20                 # Number of concurrent requests (5-20 recommended, higher = faster)
DELAY_BETWEEN_BATCHES_MS = 500  # Delay between batches in milliseconds (0-1000, lower = faster)
